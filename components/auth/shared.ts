// Re-exported from lib/ui/formStyles.ts, which other features (e.g.
// components/verification) also draw from — kept here too so existing
// imports in this folder don't need to change.
export {
  inputClasses,
  primaryButtonClasses,
  errorBannerClasses,
  successBannerClasses,
  cardClasses as authCardClasses,
} from "@/lib/ui/formStyles";
